import { defineConfig } from 'vite'
import { elementarPreset } from '@elementar/devkit/vite'

/**
 * Лендинг «элементар ос» — статическая страница на домене `elementaros`.
 *
 * PWA выключен: устанавливать концепт-документ на домашний экран незачем, а
 * сервис-воркер на том же происхождении отдавал бы старую сборку и выглядело
 * бы это как «правки не применились» (та же яма, что описана в финансере).
 *
 * `base` по умолчанию (`/`): страница лежит в корне домена, и `@font-face` из
 * дизайн-системы адресуется абсолютным `/fonts/*` — относительная база их бы
 * сломала.
 *
 * Порты 5177 / 5178 — не заняты финансером (5173 / 5174).
 */
export default defineConfig({
  ...elementarPreset({ pwa: false, port: 5177 }),
  preview: { port: 5178 },
})
